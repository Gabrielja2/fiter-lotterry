import styled from "styled-components";

export const StyledTitle = styled.div`
  display: flex;
  flex-direction: row;
  gap: 10px;

  h1:nth-child(1) {
    color: #ffffff;
    font-weight: normal;
  }
  h1:nth-child(2) {
    color: #17e72c;
    font-weight: normal;
  }
`;